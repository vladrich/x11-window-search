# * Empty lines are ignored.

# * Each line is a regexp.
#   - A mached line goes to Exclude list.
#	- A mached line that starts with ! goes to Include list
#     (! is removed from the pattern).
	  	 
# * Put \ in front of the first # or ! to escape from their meaning.
